# 安全与隐私

## 本地数据

项目列表和应用配置存放于 Tauri 应用数据目录，macOS 默认为 `~/Library/Application Support/com.yangjie.skillmanager/`。此标识符用于保持已有用户的数据兼容性。

LLM API Key 保存在其中的 `llm.json`，使用本机文件权限保护；这是本地明文文件，不是系统钥匙串或加密保险库。不要上传应用数据目录、数据库、备份或会话日志。仓库不需要任何真实密钥才能构建或运行常规测试。

项目资源的恢复状态位于对应 Git 私有目录的 `skill-manager/` 中。不要清理仍有关闭资源或未完成事务的数据。

## 网络请求

搜索、仓库读取、安装与更新会访问 skills.sh 和 GitHub。主动使用模型列表、翻译、解读或检查时，会请求用户设置的 LLM 接口，并发送所选文档内容。使用统计在本机读取 Codex 日志。将私密文档发送给外部模型前，请确认所配置服务可以接收这些内容。

## 报告问题

若 GitHub 仓库已启用私密漏洞报告，请使用 Security 页的 “Report a vulnerability”。若未启用，请只在 Issue 中请求私密联系方式，不要公开粘贴密钥、个人数据库或完整会话日志。普通问题可提交经过脱敏的复现步骤、系统版本与错误消息。

## 发布检查

`.gitignore` 排除常见密钥文件、数据库、缓存、本地预览和恢复资料；它不能阻止强制添加文件，也不能发现写在源码中的密钥。提交前仍需检查暂存内容。已泄露的密钥应撤销或轮换，单独删除文件无法消除历史泄露。
